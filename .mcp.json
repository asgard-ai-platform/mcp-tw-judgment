{
  "mcpServers": {
    "tw-judgment": {
      "command": "uv",
      "args": ["run", "mcp-tw-judgment"],
      "cwd": "${PWD}",
      "env": {
        "PYTHONPATH": "${PWD}",
        "SERVICE_API_TOKEN": "${SERVICE_API_TOKEN}"
      }
    }
  }
}
