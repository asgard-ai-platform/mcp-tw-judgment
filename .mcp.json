{
  "mcpServers": {
    "{service}": {
      "command": "uv",
      "args": ["run", "mcp-service"],
      "cwd": "${PWD}",
      "env": {
        "PYTHONPATH": "${PWD}",
        "SERVICE_API_TOKEN": "${SERVICE_API_TOKEN}"
      }
    }
  }
}
